//SQL Query for leetcode question 1667. Fix Names in a Table

SELECT user_id,
CONCAT(
    UPPER(LEFT(name,1)),
    LOWER(Substring(name,2))
) AS name
FROM USERS
ORDER BY user_id;


OUTPUT:

| user_id | name  |
| ------- | ----- |
| 1       | Alice |
| 2       | Bob   |
